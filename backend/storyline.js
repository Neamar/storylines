"use strict";


/**
 * Retrieve all events within a given `storyline` in a `storypath`
 * @cbreturn an array of JSON for each event in the storyline
 */
module.exports. = function getEventsInStoryline(storyPath, storyline) {

};

/**
 * Retrieve all the storylines available within `storyPath`
 * @cbreturn an array of each storyline slug
 */
module.exports.getAllStorylines = function getAllStorylines(storyPath, cb) {

};


/**
 * Retrieve the config for a given story
 */
module.exports.getStoryConfig = function getStoryConfig(storyPath, cb) {

};


module.exports.buildBundle = function buildBundle(storyPath, cb) {

};
